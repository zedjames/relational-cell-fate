# Data

This directory contains source metadata and, at release freeze, the paper-specific derived finite inputs required to reproduce the reported analyses.

Raw third-party datasets are not mirrored by default. See `source_manifest.tsv` and `../docs/PROVENANCE.md`.
