# Frozen result artifacts

This directory contains selected paper-facing result tables copied from the frozen analysis authority.

The release will also contain a manifest with SHA-256 hashes and a validation receipt tying these artifacts to the manuscript version archived with the DOI.
